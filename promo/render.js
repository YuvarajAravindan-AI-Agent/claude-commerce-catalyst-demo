const { chromium } = require('playwright');
const ffmpeg = require('ffmpeg-static');
const { spawn } = require('node:child_process');
const fs = require('node:fs'); const path = require('node:path');
(async()=>{const dir=path.join(__dirname,'frames');fs.mkdirSync(dir,{recursive:true});const browser=await chromium.launch({headless:true,executablePath:'/snap/bin/chromium',args:['--no-sandbox']});const page=await browser.newPage({viewport:{width:1080,height:1080},deviceScaleFactor:1});await page.goto('file://'+path.join(__dirname,'scene.html'));for(let i=0;i<640;i++){await page.screenshot({path:path.join(dir,`frame-${String(i).padStart(5,'0')}.png`)});await page.waitForTimeout(100);};await browser.close();const out='/home/yuvaraj/Documents/project-podcast-thumbnails/zoho-catalyst-3.0-promo/claude-commerce-catalyst-promo.mp4';const p=spawn(ffmpeg,['-y','-framerate','10','-i',path.join(dir,'frame-%05d.png'),'-c:v','libx264','-pix_fmt','yuv420p','-movflags','+faststart',out],{stdio:'inherit'});p.on('exit',c=>{if(c)process.exit(c);console.log(out)});})();
